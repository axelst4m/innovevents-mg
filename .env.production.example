# ==============================================
# Variables d'environnement - Production
# ==============================================
# Copier ce fichier en .env et remplir les valeurs

# --- Deploiement ---
# Nom de domaine pointe vers le VPS (utilise par Traefik pour le routage + certificat SSL)
DEPLOY_HOST=inno.st4m.fr

# Nom du reseau Docker Traefik (doit deja exister)
TRAEFIK_NETWORK=web

# --- Securite ---
# Cle secrete JWT (generer avec : openssl rand -hex 32)
JWT_SECRET=CHANGER_MOI_generer_avec_openssl_rand_hex_32

# --- Base de donnees ---
# Mot de passe PostgreSQL
POSTGRES_PASSWORD=CHANGER_MOI_mot_de_passe_fort

# --- Email (optionnel) ---
# Configuration SMTP pour l'envoi d'emails
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=contact@innovevents.com
