# Configuration pour les tests
# Utilise la meme base que le dev (dans Docker)
# Pour des tests isoles, creer une base innovevents_test

NODE_ENV=test
PORT=3001

# Base PostgreSQL (meme que dev si Docker tourne)
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/innovevents

# MongoDB
MONGO_URL=mongodb://localhost:27017/innovevents_test

# JWT
JWT_SECRET=test_secret_key_2024
